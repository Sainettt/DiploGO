version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: diplogo-postgres
    restart: always
    env_file:
      - ./api/.env
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
